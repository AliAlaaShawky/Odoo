<odoo>
  <!-- <record id="view_purchase_order_wizard_form" model="ir.ui.view">
      <field name="name">purchase.order.wizard.form</field>
      <field name="model">purchase.order.wizard</field>
      <field name="arch" type="xml">
          <form string="Purchase Order Options">
              <group>
                <field name="customer_id"/>
                  <field name="option"/>
                  <field name="order_id" invisible="option != 'existing' " domain="[('partner_id', '=', customer_id), ('sales_order_id', '=', False)]"/>
              </group>
              <group invisible="option != 'existing' ">
                <field name="order_line_ids">
                    <tree string="Order Lines" editable="bottom">
                        <field name="product_id"/>
                        <field name="product_qty"/>
                        <field name="price_unit"/>
                        <field name="price_total"/>
                    </tree>
                </field>
            </group>
            <group>
              <field name="total_price"/>

            </group>
              <footer>
                  <button string="Confirm" type="object" name="action_confirm" class="btn-primary"/>
                  <button string="Cancel" class="btn-secondary" special="cancel"/>
              </footer>
          </form>
      </field>
  </record> -->

  <!-- <record id="action_purchase_order_wizard" model="ir.actions.act_window">
      <field name="name">Purchase Order Wizard</field>
      <field name="res_model">purchase.order.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
  </record> -->
</odoo>
